spring:
  application:
    name: service-monitor
  cloud:
    config:
      uri: ${NEXTGEN_CONFIG_SERVER:http://localhost:8199}
#      username: user
#      password: s3cr3t
      fail-fast: false
      retry:
        initial-interval: 5000
        max-attempts: 24
        max-interval: 5000
      
server:
  port: ${PORT:9080}
