server:
  port: 9080

spring:
  application:
    name: service-monitor
  cloud:
    config:
      uri: http://nextgen-config:8199
      username: user
      password: ${CONFIG_SERVICE_PASSWORD:s3cr3t}
      fail-fast: true 
      
         
