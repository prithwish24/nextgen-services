spring:
  application:
    name: reservation-service
  cloud:
    config:
      uri: http://localhost:8199
      username: user
      password: ${CONFIG_SERVICE_PASSWORD:s3cr3t}
      fail-fast: true  

    
server:
  port: ${port:8002}
