spring:
  application:
    name: reservation-service
  cloud:
    config:
      uri: http://${NEXTGEN_CONFIG_SERVER:localhost}:8199
#      username: user
#      password: s3cr3t
      fail-fast: false
      retry:
        initial-interval: 5000
        max-attempts: 24
        max-interval: 5000  

    
server:
  port: ${port:8002}
