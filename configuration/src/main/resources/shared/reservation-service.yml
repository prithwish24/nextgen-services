server:
  error:
    whitelabel:
      enabled: true

eureka:
  client:
    register-with-eureka: true
    serviceUrl:
        defaultZone: http://127.0.0.1:8088/eureka/
          
logging:
  level:
     org:
       springframework:
         security: DEBUG 

hystrix:
  metrics:
    enabled: true               
