server:
    error:
      whitelabel:
        enabled: true
    
eureka:
  instance:
    hostname: ${NEXTGEN_REGISTRY_SERVER:localhost}
  client:
    serviceUrl:
        defaultZone: http://${eureka.instance.hostname}:8088/eureka/
          
logging:
  level:
     org:
       springframework:
         security: DEBUG  

hystrix:
  metrics:
    enabled: true         
