spring:
  application:
    name: service-gateway
  cloud:
    config:
      uri: ${NEXTGEN_CONFIG_SERVER:http://localhost:8199}
#      username: user
#      password: s3cr3t
      fail-fast: false
      retry:
        initial-interval: 10000
        max-attempts: 5
        max-interval: 10000
      
server:
  port: ${PORT:8080}  

