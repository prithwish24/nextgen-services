spring:
  application:
    name: service-gateway
  cloud:
    config:
      uri: http://nextgen-config:8199
      username: user
      password: s3cr3t
      fail-fast: true
      
server:
  port: ${port:8080}  

