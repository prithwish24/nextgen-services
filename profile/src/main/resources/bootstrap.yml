spring:
  application:
    name: profile-service
  cloud:
    config:
      uri: http://127.0.0.1:8199
      username: user
      password: s3cr3t
      fail-fast: true

server:
    port: ${port:8001} 
